# Can-Bus-Simulator
A software-based Controller Area Network (CAN) simulation framework built in Python. Instead of relying on expensive hardware interfaces, this project emulates a multi-node automotive network entirely in software. It bridges the gap between raw hardware-level hex payloads and human-readable engineering telemetry.
